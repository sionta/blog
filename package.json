{
  "name": "blog",
  "version": "1.0.0",
  "description": "Personal site",
  "scripts": {
    "serve": "bundle exec jekyll serve",
    "clean": "bundle exec jekyll clean",
    "build": "bundle exec jekyll build",
    "test": "bundle exec jekyll clean && bundle exec jekyll serve --watch --trace"
  },
  "dependencies": {
    "clean-css": "^5.3.3",
    "clean-css-cli": "^5.6.3",
    "html-minifier": "^4.0.0",
    "uglify-js": "^3.18.0"
  }
}
