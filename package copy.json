{
  "name": "blog",
  "version": "1.0.0",
  "description": "Personal site",
  "scripts": {
    "serve": "bundle exec jekyll serve --watch",
    "build": "bundle exec jekyll build && npm run minify",
    "build-clean": "bundle exec jekyll clean && npm run build",
    "minify": "node scripts/minify-all.js"
  },
  "devDependencies": {
    "clean-css-cli": "^5.6.3",
    "html-minifier": "^4.0.0",
    "uglify-js": "^3.18.0"
  }
}
